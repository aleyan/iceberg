# Selected Python iceberg items, ordered from the top of the iceberg to the bottom.
# Names and descriptions are derived from candidates.toml by stable candidate slug.
# Ratings are arithmetic means of GPT, Claude, and Gemini scores.
schema_version = 1
item_count = 110
ratings_models = ["gpt-5.6-sol", "claude-opus-5", "gemini-3.8-flash"]

[[items]]
slug = "eval-and-exec-are-not-sandboxes"
name = "`eval()` and `exec()` are not sandboxes"
short_description = "`eval()` evaluates a Python expression and `exec()` executes Python statements. Neither is a sandbox: removing or replacing `__builtins__` is not a reliable security boundary, so passing either function untrusted input is a code-execution vulnerability."
url = "https://docs.python.org/3.14/library/functions.html#eval"
obscurity_bucket = 1
obscurity_rating = 2.73
cursedness_rating = 7.93

[[items]]
slug = "mutable-default-arguments-are-shared"
name = "Mutable default arguments"
short_description = "Default argument expressions are evaluated once, when the function is defined. A list, dictionary, or other mutable default therefore persists across every call that omits that argument."
url = "https://docs.python.org/3.14/faq/programming.html#why-are-default-values-shared-between-objects"
obscurity_bucket = 1
obscurity_rating = 1.60
cursedness_rating = 6.70

[[items]]
slug = "sequence-repetition-aliases-nested-objects"
name = "The nested-list aliasing trap"
short_description = "`[[]] * 3` does not create three lists; it repeats the same reference three times. Mutating one apparent row mutates every row, a classic source of corrupted matrices and defaults."
url = "https://docs.python.org/3.14/library/stdtypes.html#common-sequence-operations"
obscurity_bucket = 1
obscurity_rating = 2.60
cursedness_rating = 6.43

[[items]]
slug = "closures-capture-variables-not-values"
name = "Late-binding closures"
short_description = "Functions created in a loop usually read the loop variable later, after the loop has finished. They therefore all see its final value unless the value is bound separately."
url = "https://docs.python.org/3.14/faq/programming.html#why-do-lambdas-defined-in-a-loop-with-different-values-all-return-the-same-result"
obscurity_bucket = 1
obscurity_rating = 2.27
cursedness_rating = 6.37

[[items]]
slug = "zip-silently-truncates"
name = "Silent `zip()` truncation"
short_description = "By default, `zip(a, b)` stops when either iterable ends and says nothing about extra data. Length mismatches can quietly drop records unless `strict=True` is used."
url = "https://docs.python.org/3.14/library/functions.html#zip"
obscurity_bucket = 1
obscurity_rating = 2.20
cursedness_rating = 6.10

[[items]]
slug = "nan-is-not-equal-to-itself"
name = "NaN non-reflexivity and collection surprises"
short_description = "Floating-point NaN compares unequal to every value, including itself. It can also behave awkwardly in sets, dictionary keys, sorting, deduplication, and tests that assume reflexive equality."
url = "https://docs.python.org/3.14/library/stdtypes.html#comparisons"
obscurity_bucket = 1
obscurity_rating = 2.33
cursedness_rating = 5.87

[[items]]
slug = "round-uses-ties-to-even-on-binary-floats"
name = "Banker’s rounding and binary floats"
short_description = "Exact halfway cases round to the nearest even result, and many decimal-looking values are not exact binary halves. Thus familiar results such as `round(2.675, 2) == 2.67` can look wrong."
url = "https://docs.python.org/3.14/library/functions.html#round"
obscurity_bucket = 1
obscurity_rating = 2.73
cursedness_rating = 5.53

[[items]]
slug = "for-loop-target-leaks-after-the-loop"
name = "Loop-variable leakage"
short_description = "After `for i in range(3): ...`, `i` is `2` in the enclosing scope. Comprehensions do *not* leak in Python 3. Closures over `i` after the loop all see the final value."
url = "https://docs.python.org/3/reference/compound_stmts.html#the-for-statement"
obscurity_bucket = 1
obscurity_rating = 2.20
cursedness_rating = 4.93

[[items]]
slug = "floating-point-is-not-exact"
name = "Binary floating-point imprecision"
short_description = "`0.1 + 0.2 == 0.3` evaluates to `False` due to IEEE-754 binary floating-point representation limits."
url = "https://docs.python.org/3/tutorial/floatingpoint.html"
obscurity_bucket = 1
obscurity_rating = 2.17
cursedness_rating = 4.87

[[items]]
slug = "one-item-tuple-is-made-by-the-comma"
name = "The singleton-tuple comma"
short_description = "Parentheses merely group expressions; the comma constructs the tuple. `(value)` is just `value`, while `value,` is a one-element tuple even without parentheses."
url = "https://docs.python.org/3.14/reference/expressions.html#expression-lists"
obscurity_bucket = 1
obscurity_rating = 1.90
cursedness_rating = 4.73

[[items]]
slug = "pickle-deserialization-runs-code"
name = "Pickle remote code execution"
short_description = "Pickle is a program-like object reconstruction format, not safe data serialization. Unpickling attacker-controlled bytes can import globals and invoke callables, leading directly to arbitrary code execution."
url = "https://docs.python.org/3.14/library/pickle.html#module-pickle"
obscurity_bucket = 2
obscurity_rating = 3.37
cursedness_rating = 8.57

[[items]]
slug = "shell-true-enables-command-injection"
name = "`shell=True` command injection"
short_description = "With `shell=True`, the application is responsible for correctly quoting whitespace and shell metacharacters. Incorporating untrusted text into the command can turn data into additional commands or redirections."
url = "https://docs.python.org/3.14/library/subprocess.html#security-considerations"
obscurity_bucket = 2
obscurity_rating = 3.60
cursedness_rating = 8.43

[[items]]
slug = "adjacent-string-literals-concatenate"
name = "Implicit string-literal concatenation"
short_description = "String literals separated only by whitespace are joined at compile time. A missing comma between two strings in a list therefore produces one combined element without a syntax error."
url = "https://docs.python.org/3.14/reference/lexical_analysis.html#string-literal-concatenation"
obscurity_bucket = 2
obscurity_rating = 3.47
cursedness_rating = 6.43

[[items]]
slug = "bool-is-int-and-collides-with-one"
name = "The `bool`–`int` collision"
short_description = "Numeric values that compare equal have the same hash and occupy one dictionary or set slot. Inserting `True`, `1`, and `1.0` therefore cannot create three independent keys; later writes replace the shared entry's value."
url = "https://docs.python.org/3.14/library/stdtypes.html#mapping-types-dict"
obscurity_bucket = 2
obscurity_rating = 3.37
cursedness_rating = 6.30

[[items]]
slug = "string-interning-makes-is-misleading"
name = "The string-interning identity trap"
short_description = "Implementations may reuse immutable objects such as small integers or some strings. Identity comparisons can therefore appear to work in one expression, session, or build and fail in another; use `==` for values."
url = "https://docs.python.org/3.14/reference/datamodel.html#objects-values-and-types"
obscurity_bucket = 2
obscurity_rating = 3.13
cursedness_rating = 6.30

[[items]]
slug = "assignment-makes-a-name-local-throughout-a-function"
name = "The `UnboundLocalError` scope trap"
short_description = "If any binding operation for a name appears in a function body, every unqualified use of that name in the block is treated as local. A read before the assignment raises `UnboundLocalError` instead of reading the global."
url = "https://docs.python.org/3.14/reference/executionmodel.html#resolution-of-names"
obscurity_bucket = 2
obscurity_rating = 3.20
cursedness_rating = 6.07

[[items]]
slug = "mutating-a-collection-during-iteration"
name = "Mutation during iteration"
short_description = "`for x in items: items.remove(x)` skips elements. Growing the list can infinite-loop. Iterate a copy, or build a new list."
url = "https://docs.python.org/3/reference/compound_stmts.html#the-for-statement"
obscurity_bucket = 2
obscurity_rating = 3.07
cursedness_rating = 6.03

[[items]]
slug = "mutable-class-attributes-are-shared"
name = "Shared mutable class attributes"
short_description = "`class C: items = []` then `C().items.append(1)` mutates the class attribute. Every instance shares it unless `__init__` makes a new list. Looks like a default field. Isn’t."
url = "https://docs.python.org/3/tutorial/classes.html#class-and-instance-variables"
obscurity_bucket = 2
obscurity_rating = 3.33
cursedness_rating = 5.97

[[items]]
slug = "calling-async-does-not-run-it"
name = "The unawaited-coroutine trap"
short_description = "Calling an `async def` function merely creates a coroutine object. Unless it is awaited or scheduled, none of its body runs; dropping it generally produces only a runtime warning."
url = "https://docs.python.org/3.14/library/asyncio-task.html#coroutines"
obscurity_bucket = 2
obscurity_rating = 3.30
cursedness_rating = 5.60

[[items]]
slug = "shadowing-builtins-breaks-later-lookups"
name = "Built-in shadowing"
short_description = "Assigning `id = 5` or `list = [1, 2]` replaces the built-in functional callables within local scope without raising an error."
url = "https://docs.python.org/3/reference/executionmodel.html#naming-and-binding"
obscurity_bucket = 2
obscurity_rating = 3.33
cursedness_rating = 5.57

[[items]]
slug = "assertions-disappear-under-optimization"
name = "Optimized-away assertions"
short_description = "Compiling or running with optimization removes assert statements entirely. Assertions therefore cannot safely perform required work or enforce authorization, validation, invariants, or other production behavior."
url = "https://docs.python.org/3.14/reference/simple_stmts.html#the-assert-statement"
obscurity_bucket = 3
obscurity_rating = 3.80
cursedness_rating = 7.57

[[items]]
slug = "script-directory-can-shadow-standard-library"
name = "Standard-library module shadowing"
short_description = "Import search paths commonly place the script directory and user-supplied paths before standard-library locations. A local `json.py`, `email.py`, or malicious look-alike can be imported instead of the expected module."
url = "https://docs.python.org/3.14/library/sys_path_init.html"
obscurity_bucket = 3
obscurity_rating = 4.00
cursedness_rating = 7.30

[[items]]
slug = "random-is-not-cryptographically-secure"
name = "The insecure-PRNG trap"
short_description = "The default generator is deterministic Mersenne Twister, designed for simulation rather than adversaries. Tokens, reset links, session identifiers, and cryptographic material must use `secrets` or another secure source."
url = "https://docs.python.org/3.14/library/random.html"
obscurity_bucket = 3
obscurity_rating = 3.70
cursedness_rating = 7.10

[[items]]
slug = "gil-does-not-make-code-parallel-or-thread-safe"
name = "The GIL provides neither CPU parallelism nor application-level thread safety"
short_description = "Even traditional CPython requires synchronization around multi-step invariants and shared mutable state. Optional free-threaded builds remove the GIL, while current built-in locking remains an implementation detail rather than an application-level contract."
url = "https://docs.python.org/3.14/howto/free-threading-python.html#thread-safety"
obscurity_bucket = 3
obscurity_rating = 4.07
cursedness_rating = 7.07

[[items]]
slug = "bare-except-catches-process-exits"
name = "Overbroad bare `except`"
short_description = "`except:` (or `except BaseException`) swallows Ctrl-C and `sys.exit()`. The process refuses to die. `except Exception` is the less nuclear option; still broad."
url = "https://docs.python.org/3/library/exceptions.html#exception-hierarchy"
obscurity_bucket = 3
obscurity_rating = 3.87
cursedness_rating = 6.83

[[items]]
slug = "imports-execute-code-and-can-expose-partial-modules"
name = "Imports execute code and circular imports expose partial modules"
short_description = "Importing a module executes its top-level statements. The module is placed in `sys.modules` before execution finishes, so circular imports can observe a half-initialized object with names missing or not yet valid."
url = "https://docs.python.org/3.14/reference/import.html"
obscurity_bucket = 3
obscurity_rating = 3.87
cursedness_rating = 6.50

[[items]]
slug = "multiprocessing-spawn-needs-main-guard"
name = "The multiprocessing `__main__` guard"
short_description = "With the spawn start method the child re-imports your `__main__` module. Without `if __name__ == '__main__':` that re-import starts more children, recursively, until something gives out."
url = "https://docs.python.org/3/library/multiprocessing.html#the-spawn-and-forkserver-start-methods"
obscurity_bucket = 3
obscurity_rating = 4.03
cursedness_rating = 6.40

[[items]]
slug = "strip-takes-a-character-set-not-a-suffix"
name = "The `strip()` character-set trap"
short_description = "`'example.com'.strip('.com')` returns `'exampl'`, because it removes any of those characters from both ends. `removeprefix` and `removesuffix` only arrived in 3.9."
url = "https://docs.python.org/3/library/stdtypes.html#str.strip"
obscurity_bucket = 3
obscurity_rating = 4.00
cursedness_rating = 6.27

[[items]]
slug = "pip-installed-into-a-different-interpreter"
name = "The wrong-`pip` problem"
short_description = "`pip install x` succeeds; `python script.py` says `ModuleNotFoundError`. Two Pythons, two site-packages. `sys.executable` vs `pip -V` is the autopsy."
url = "https://docs.python.org/3/installing/index.html"
obscurity_bucket = 3
obscurity_rating = 3.87
cursedness_rating = 6.27

[[items]]
slug = "multiprocessing-pickles-your-callable"
name = "The multiprocessing pickling boundary"
short_description = "`Process` / `Pool` send the target over pickle. Nested functions, lambdas, and unpicklable closures fail in ugly ways. Windows spawn is stricter than Linux fork."
url = "https://docs.python.org/3/library/multiprocessing.html#the-spawn-and-forkserver-start-methods"
obscurity_bucket = 3
obscurity_rating = 4.07
cursedness_rating = 6.10

[[items]]
slug = "unsafe-yaml-load-can-construct-objects"
name = "Unsafe legacy `yaml.load()`"
short_description = "PyYAML's default loader honoured tags like `!!python/object/apply`, making any YAML file a code-execution vector. It took until version 5.1 for the default to change and a warning to appear."
url = "https://pyyaml.org/wiki/PyYAMLDocumentation"
obscurity_bucket = 4
obscurity_rating = 4.47
cursedness_rating = 8.60

[[items]]
slug = "python-2-input-evaluated-user-input"
name = "Python 2 `input()` code execution"
short_description = "`input()` read a line and passed it to `eval()`. Anyone answering a prompt could run arbitrary code; `raw_input()` was the safe one, and the names were swapped in Python 3."
url = "https://docs.python.org/2/library/functions.html#input"
obscurity_bucket = 4
obscurity_rating = 4.73
cursedness_rating = 7.83

[[items]]
slug = "mktemp-has-a-race-condition"
name = "The `mktemp()` TOCTOU race"
short_description = "`mktemp()` returns a name without atomically creating the file. Another process can create or redirect that path before it is opened, enabling overwrite, disclosure, or privilege-escalation attacks."
url = "https://docs.python.org/3.14/library/tempfile.html#tempfile.mktemp"
obscurity_bucket = 4
obscurity_rating = 4.40
cursedness_rating = 7.83

[[items]]
slug = "finally-overrides-control-flow"
name = "The `finally` control-flow trap"
short_description = "A `return`, `break`, or `continue` executed inside `finally` discards any saved exception. A `return` there also overrides a value returned from the `try` suite."
url = "https://docs.python.org/3.14/reference/compound_stmts.html#the-try-statement"
obscurity_bucket = 4
obscurity_rating = 4.27
cursedness_rating = 7.30

[[items]]
slug = "assert-with-a-comma-is-always-true"
name = "The always-true tuple assertion"
short_description = "`assert (x > 0, \"x must be positive\")` asserts a two-element tuple, which is always truthy. The test never fails. Python 3.12 finally added a SyntaxWarning for it."
url = "https://docs.python.org/3/reference/simple_stmts.html#the-assert-statement"
obscurity_bucket = 4
obscurity_rating = 4.77
cursedness_rating = 7.30

[[items]]
slug = "open-default-encoding-depends-on-environment"
name = "The default-encoding trap"
short_description = "Text mode used the platform's preferred encoding by default, so the same script read a UTF-8 file correctly on Linux and mangled it on a Japanese Windows machine. PEP 686 is still working through making UTF-8 the default."
url = "https://peps.python.org/pep-0686/"
obscurity_bucket = 4
obscurity_rating = 4.47
cursedness_rating = 7.17

[[items]]
slug = "os-path-join-throws-away-everything-before-an-absolute-path"
name = "Absolute-path reset in `os.path.join()`"
short_description = "`os.path.join('/safe/dir', user_input)` returns just `user_input` if it starts with a slash. The same rule applies to `pathlib`'s `/` operator."
url = "https://docs.python.org/3/library/os.path.html#os.path.join"
obscurity_bucket = 4
obscurity_rating = 4.57
cursedness_rating = 7.07

[[items]]
slug = "daemon-threads-stop-abruptly"
name = "Abrupt daemon-thread shutdown"
short_description = "The interpreter exits when only daemon threads remain. Their resources may not be released, transactions may not finish, and `finally` blocks may never run; graceful shutdown requires non-daemon threads and signaling."
url = "https://docs.python.org/3.14/library/threading.html#thread-objects"
obscurity_bucket = 4
obscurity_rating = 4.53
cursedness_rating = 6.87

[[items]]
slug = "tuple-augmented-assignment-mutates-before-failing"
name = "The tuple `+=` paradox"
short_description = "Augmented assignment first performs the in-place mutation, then tries to store the result back into the tuple. The list inside the tuple changes even though the final store raises `TypeError`."
url = "https://docs.python.org/3.14/faq/programming.html#why-does-a-tuple-i-item-raise-an-exception-when-the-addition-works"
obscurity_bucket = 4
obscurity_rating = 4.27
cursedness_rating = 6.80

[[items]]
slug = "dict-fromkeys-reuses-one-mutable-value"
name = "The `dict.fromkeys()` aliasing trap"
short_description = "`dict.fromkeys(keys, value)` assigns the exact same value object to every key. Passing a list or dictionary means a mutation through one entry appears under all entries."
url = "https://docs.python.org/3.14/library/stdtypes.html#dict.fromkeys"
obscurity_bucket = 4
obscurity_rating = 4.37
cursedness_rating = 6.53

[[items]]
slug = "xml-parsers-face-expansion-bombs"
name = "XML parser resource-exhaustion attacks"
short_description = "Python's XML documentation warns about entity-expansion bombs, quadratic blowups, decompression bombs, huge tokens, external entities, and related attacks. A tiny document can consume enormous CPU, memory, bandwidth, or disk."
url = "https://docs.python.org/3.14/library/xml.html#xml-vulnerabilities"
obscurity_bucket = 5
obscurity_rating = 4.97
cursedness_rating = 7.67

[[items]]
slug = "regular-expressions-have-no-timeout"
name = "Catastrophic regex backtracking (ReDoS)"
short_description = "The `re` engine backtracks, so a pattern like `(a+)+b` against a long run of a's takes exponential time. There is no way to bound it; the request just never finishes."
url = "https://docs.python.org/3/library/re.html"
obscurity_bucket = 5
obscurity_rating = 5.23
cursedness_rating = 7.50

[[items]]
slug = "high-recursion-limit-can-crash"
name = "The recursion-limit crash"
short_description = "`sys.setrecursionlimit()` protects the C stack as well as Python code. Raising it beyond what the platform can support can replace a friendly `RecursionError` with a process crash."
url = "https://docs.python.org/3.14/library/sys.html#sys.setrecursionlimit"
obscurity_bucket = 5
obscurity_rating = 5.10
cursedness_rating = 7.50

[[items]]
slug = "utcnow-returned-a-lying-object"
name = "The naïve-UTC `utcnow()` trap"
short_description = "`datetime.utcnow()` returned a timezone-naive object containing a UTC clock reading. Code that treated that naive value as local time could compare or convert it incorrectly; the method was deprecated in Python 3.12 in favor of `datetime.now(timezone.utc)`."
url = "https://docs.python.org/3/library/datetime.html#datetime.datetime.utcnow"
obscurity_bucket = 5
obscurity_rating = 5.23
cursedness_rating = 7.47

[[items]]
slug = "bare-match-name-captures-instead-of-comparing"
name = "The match/case capture trap"
short_description = "In structural pattern matching, an unqualified name is a capture pattern, not a comparison with an existing variable. It matches every subject and binds the name, often making later cases unreachable."
url = "https://docs.python.org/3.14/reference/compound_stmts.html#capture-patterns"
obscurity_bucket = 5
obscurity_rating = 5.07
cursedness_rating = 6.87

[[items]]
slug = "reading-an-attribute-can-execute-arbitrary-code"
name = "Descriptor execution on attribute access"
short_description = "Descriptors power properties, methods, slots, and many framework fields. A seemingly harmless `obj.attr` read may call user code, mutate state, block, perform I/O, or raise an unrelated exception."
url = "https://docs.python.org/3.14/reference/datamodel.html#implementing-descriptors"
obscurity_bucket = 5
obscurity_rating = 4.93
cursedness_rating = 6.87

[[items]]
slug = "lru-cache-keeps-strong-references"
name = "The `lru_cache` retention trap"
short_description = "Cached arguments and return values remain strongly referenced until eviction or cache clearing. Decorating an instance method makes each distinct `self` part of the cache key and can keep entire object graphs alive."
url = "https://docs.python.org/3.14/library/functools.html#functools.lru_cache"
obscurity_bucket = 5
obscurity_rating = 5.23
cursedness_rating = 6.83

[[items]]
slug = "datetime-is-a-subclass-of-date"
name = "The `datetime`–`date` inheritance flaw"
short_description = "`isinstance(datetime.now(), date)` is True, so a datetime passes every date check, then compares unequal to the date it represents and raises when subtracted from one."
url = "https://docs.python.org/3/library/datetime.html#datetime.datetime"
obscurity_bucket = 5
obscurity_rating = 5.20
cursedness_rating = 6.53

[[items]]
slug = "enum-members-with-equal-values-become-aliases"
name = "Enum value aliasing"
short_description = "Two members that share a value are not two members: the second is an alias for the first. It vanishes from iteration, and any lookup returns the earlier name."
url = "https://docs.python.org/3/library/enum.html"
obscurity_bucket = 5
obscurity_rating = 5.23
cursedness_rating = 6.10

[[items]]
slug = "stopiteration-escaping-a-generator-becomes-runtimeerror"
name = "PEP 479’s `StopIteration` rewrite"
short_description = "A `StopIteration` accidentally raised inside generator code is transformed into `RuntimeError` rather than ending iteration. The rule prevents silent truncation, but makes old generator idioms fail in a surprising place."
url = "https://peps.python.org/pep-0479/"
obscurity_bucket = 5
obscurity_rating = 5.23
cursedness_rating = 6.07

[[items]]
slug = "asyncio-only-weakly-references-tasks"
name = "Disappearing asyncio tasks"
short_description = "`asyncio.create_task()` returns an object that must be retained. The event loop holds only weak references, so an otherwise unreferenced task may be garbage-collected before it finishes—even in mid-execution."
url = "https://docs.python.org/3.14/library/asyncio-task.html#asyncio.create_task"
obscurity_bucket = 6
obscurity_rating = 5.77
cursedness_rating = 8.30

[[items]]
slug = "popen-deadlocks-if-you-read-the-pipes-yourself"
name = "The subprocess pipe deadlock"
short_description = "A child writing more than the OS pipe buffer blocks until someone drains it. If you `wait()` before reading, both processes stop forever. This is why `communicate()` exists."
url = "https://docs.python.org/3/library/subprocess.html#subprocess.Popen.wait"
obscurity_bucket = 6
obscurity_rating = 5.33
cursedness_rating = 7.87

[[items]]
slug = "dynamic-builtins-overwriting"
name = "Process-wide built-in poisoning"
short_description = "You can reassign primitives directly at runtime (`builtins.int = str`), changing type conversions across the entire global runtime."
url = "https://docs.python.org/3/library/builtins.html"
obscurity_bucket = 6
obscurity_rating = 5.57
cursedness_rating = 7.73

[[items]]
slug = "del-finalizers-are-unreliable-and-reentrant"
name = "The `__del__` finalization trap"
short_description = "A finalizer can run while arbitrary code is executing, including from another thread. It can deadlock on locks already held, and exceptions raised inside it are merely printed to `stderr` and otherwise ignored."
url = "https://docs.python.org/3.14/reference/datamodel.html#object.__del__"
obscurity_bucket = 6
obscurity_rating = 5.60
cursedness_rating = 7.67

[[items]]
slug = "asyncio-cancellederror-is-a-baseexception"
name = "Cancellation escapes `except Exception`"
short_description = "Cancellation normally bypasses `except Exception:` because `CancelledError` directly subclasses `BaseException`. Cleanup should use `finally`, and swallowed cancellation can break structured-concurrency components."
url = "https://docs.python.org/3.14/library/asyncio-exceptions.html#asyncio.CancelledError"
obscurity_bucket = 6
obscurity_rating = 5.67
cursedness_rating = 7.27

[[items]]
slug = "asyncio-gather-does-not-cancel-siblings-after-one-fails"
name = "`gather()` sibling non-cancellation"
short_description = "With default options, the first raised exception is propagated to the waiter, but other submitted awaitables keep running. Treating the failed `gather` call as an all-or-nothing operation can leak side effects."
url = "https://docs.python.org/3.14/library/asyncio-task.html#asyncio.gather"
obscurity_bucket = 6
obscurity_rating = 5.80
cursedness_rating = 7.17

[[items]]
slug = "tracebacks-and-exception-context-retain-locals"
name = "Traceback reference cycles"
short_description = "An exception traceback references stack frames, and those frames reference local variables. Keeping exceptions or tracebacks can therefore retain large object graphs, secrets, files, or database resources much longer than expected."
url = "https://docs.python.org/3.14/library/traceback.html#traceback.clear_frames"
obscurity_bucket = 6
obscurity_rating = 5.57
cursedness_rating = 7.17

[[items]]
slug = "mutating-a-keys-hash-or-equality-can-make-it-unreachable"
name = "The mutable-dictionary-key trap"
short_description = "Dictionary and set lookup assumes a key's hash and equality behavior remain stable while stored. If those depend on mutable state, the object may still be present yet no longer findable or removable normally."
url = "https://docs.python.org/3.14/reference/datamodel.html#object.__hash__"
obscurity_bucket = 6
obscurity_rating = 5.30
cursedness_rating = 7.00

[[items]]
slug = "hasattr-can-hide-bugs-inside-properties"
name = "The `hasattr()` descriptor trap"
short_description = "`hasattr(obj, name)` calls `getattr` and returns `False` whenever it receives `AttributeError`. If property code accidentally raises that exception internally, the bug can masquerade as a missing attribute."
url = "https://docs.python.org/3.14/library/functions.html#hasattr"
obscurity_bucket = 6
obscurity_rating = 5.47
cursedness_rating = 6.87

[[items]]
slug = "sys-exit-in-a-worker-thread-exits-only-that-thread"
name = "`sys.exit()` in a worker thread exits only that thread"
short_description = "`sys.exit()` merely raises `SystemExit`. It terminates the process only when that exception reaches the top of the main thread; from another thread, it normally ends that one thread while the rest of the program keeps running."
url = "https://docs.python.org/3.14/library/sys.html#sys.exit"
obscurity_bucket = 6
obscurity_rating = 5.43
cursedness_rating = 6.87

[[items]]
slug = "ctypes-can-rewrite-cached-integers"
name = "Small-integer cache corruption"
short_description = "CPython caches small integers as singletons. With `ctypes` you can reach into that cache and overwrite the value stored in the object for 2, after which arithmetic across the whole process quietly produces different answers."
url = "https://docs.python.org/3/library/ctypes.html"
obscurity_bucket = 7
obscurity_rating = 6.13
cursedness_rating = 9.37

[[items]]
slug = "fork-in-a-threaded-process-deadlocks"
name = "The fork-with-threads deadlock"
short_description = "The child gets one thread but inherits every lock in whatever state it was in, so a lock held by a thread that no longer exists is held forever. Common victims: logging, the import lock, and malloc."
url = "https://docs.python.org/3/library/os.html#os.fork"
obscurity_bucket = 7
obscurity_rating = 5.90
cursedness_rating = 8.87

[[items]]
slug = "format-strings-can-expose-object-internals"
name = "Format-string attribute traversal"
short_description = "`str.format` permits attribute access in the template. A user-supplied format string like `{0.__class__.__init__.__globals__}` reaches the module globals of any object you pass in, including config and credentials."
url = "https://docs.python.org/3/library/string.html#format-string-syntax"
obscurity_bucket = 7
obscurity_rating = 5.90
cursedness_rating = 8.40

[[items]]
slug = "urljoin-can-replace-your-trusted-host"
name = "The `urljoin()` host-override trap"
short_description = "An absolute attacker-controlled second argument replaces the base URL's scheme and host. Code that appends usernames, redirect targets, or scraped links can unknowingly send requests or credentials to another origin."
url = "https://docs.python.org/3.14/library/urllib.parse.html#urllib.parse.urljoin"
obscurity_bucket = 7
obscurity_rating = 6.40
cursedness_rating = 8.13

[[items]]
slug = "ctypes-pointer-indexing-has-no-bounds"
name = "Unbounded `ctypes` pointer indexing"
short_description = "A `ctypes` pointer carries no length. Indexing or iterating past valid storage is not checked; without an external size or sentinel, code can read arbitrary memory, return garbage, or crash the interpreter process."
url = "https://docs.python.org/3.14/library/ctypes.html#pointers"
obscurity_bucket = 7
obscurity_rating = 6.40
cursedness_rating = 8.07

[[items]]
slug = "unicode-identifiers-can-confuse-reviewers"
name = "Trojan Source and Unicode confusables"
short_description = "Identifiers are normalized with NFKC, making some visibly different spellings identical, while many look-alike characters remain distinct. Bidirectional and confusable characters can make reviewed source appear to say something else."
url = "https://peps.python.org/pep-0672/"
obscurity_bucket = 7
obscurity_rating = 6.20
cursedness_rating = 8.00

[[items]]
slug = "tar-extraction-remains-dangerous"
name = "Tar extraction hazards"
short_description = "Python 3.14 defaults to the safer `data` extraction filter, but the documentation still requires prior inspection and resource limits for untrusted archives. Filters do not prevent denial-of-service payloads or every platform-specific trick."
url = "https://docs.python.org/3.14/library/tarfile.html#extraction-filters"
obscurity_bucket = 7
obscurity_rating = 5.90
cursedness_rating = 7.87

[[items]]
slug = "class-pollution-via-kwargs"
name = "Python class pollution via `**kwargs`"
short_description = "One unguarded `setattr()` loop can let a JSON body set `is_admin` directly — mass assignment vulnerabilities are alive and well in Python."
url = "https://class-pollution.github.io/"
obscurity_bucket = 7
obscurity_rating = 5.97
cursedness_rating = 7.73

[[items]]
slug = "simplehttprequesthandler-follows-symlinks-outside-its-served-directory"
name = "`SimpleHTTPRequestHandler` symlink escape"
short_description = "The handler follows symbolic links while translating request paths. A symlink placed under the apparent document root can expose files elsewhere on the host, one reason the module is explicitly unsuitable for serving untrusted content."
url = "https://docs.python.org/3.14/library/http.server.html#security-considerations"
obscurity_bucket = 7
obscurity_rating = 6.47
cursedness_rating = 7.53

[[items]]
slug = "contextlib-chdir-is-process-global-despite-looking-lexical"
name = "Process-global `contextlib.chdir()`"
short_description = "The context manager restores the old directory on exit, but changing the current directory affects the entire process. The docs call it non-parallel-safe and warn against yielding, awaiting, or switching threads while it is active."
url = "https://docs.python.org/3.14/library/contextlib.html#contextlib.chdir"
obscurity_bucket = 7
obscurity_rating = 6.20
cursedness_rating = 7.40

[[items]]
slug = "function-internals-are-writable"
name = "Writable function internals"
short_description = "Python function objects expose writable `__code__`, `__defaults__`, and `__kwdefaults__` attributes. Replacing them can change a function’s body or default behavior at runtime behind every existing reference to that function."
url = "https://docs.python.org/3/library/inspect.html"
obscurity_bucket = 7
obscurity_rating = 6.27
cursedness_rating = 7.30

[[items]]
slug = "multiprocessing-value-does-not-make-atomic"
name = "Non-atomic `multiprocessing.Value` updates"
short_description = "The synchronized wrapper protects each individual load and store, not a read-modify-write expression. `counter.value += 1` can lose updates even though the object has a lock; callers must explicitly hold `counter.get_lock()` around the compound operation."
url = "https://docs.python.org/3.14/library/multiprocessing.html#synchronized-wrapper-objects"
obscurity_bucket = 7
obscurity_rating = 5.90
cursedness_rating = 7.13

[[items]]
slug = "contextmanager-can-suppress-exceptions"
name = "The `@contextmanager` suppression trap"
short_description = "A generator-based context manager must re-raise exceptions it catches only for logging. If it merely resumes and exits, `contextlib` interprets that as successful handling and suppresses the exception."
url = "https://docs.python.org/3.14/library/contextlib.html#contextlib.contextmanager"
obscurity_bucket = 7
obscurity_rating = 6.27
cursedness_rating = 7.13

[[items]]
slug = "site-startup-files-can-execute-code"
name = "`.pth` and `sitecustomize` startup execution"
short_description = "Python's site initialization processes executable `import` lines in path-configuration files and then attempts to import customization modules. Merely starting Python in an affected environment can therefore run installed code."
url = "https://docs.python.org/3.14/library/site.html"
obscurity_bucket = 8
obscurity_rating = 6.53
cursedness_rating = 8.83

[[items]]
slug = "multiprocessing-queue-serializes-in-a-background-thread-and-can-hang-or-corrupt"
name = "The multiprocessing Queue feeder-thread trap"
short_description = "Queue writes are pickled and flushed by a feeder thread, so visibility can lag behind `put()`. Killing a process during queue use can corrupt the pipe, and joining a producer before draining its buffered data can deadlock."
url = "https://docs.python.org/3.14/library/multiprocessing.html#pipes-and-queues"
obscurity_bucket = 8
obscurity_rating = 6.90
cursedness_rating = 8.13

[[items]]
slug = "socketserver-shutdown-deadlocks-when-called-from-the-serving-thread"
name = "The `socketserver` self-shutdown deadlock"
short_description = "`BaseServer.shutdown()` waits for `serve_forever()` to finish and therefore must be called from a different thread. Calling it from the same thread that is running the server loop blocks forever by design."
url = "https://docs.python.org/3.14/library/socketserver.html#socketserver.BaseServer.shutdown"
obscurity_bucket = 8
obscurity_rating = 6.97
cursedness_rating = 7.73

[[items]]
slug = "same-zone-datetime-subtraction-ignores-dst-elapsed-time"
name = "The same-zone DST arithmetic trap"
short_description = "When two aware datetimes carry the same `tzinfo` object, subtraction uses wall-clock fields and ignores their UTC offsets. Across a DST transition, a reported twelve-hour difference can therefore represent only eleven—or as many as thirteen—elapsed hours."
url = "https://blog.ganssle.io/articles/2018/02/aware-datetime-arithmetic.html"
obscurity_bucket = 8
obscurity_rating = 6.47
cursedness_rating = 7.50

[[items]]
slug = "locale-apis-mutate-process-global-state-and-are-not-thread-safe"
name = "Process-global locale races"
short_description = "The C locale belongs to the entire process, not a Python thread. `setlocale()` changes behavior globally, and even some query functions temporarily switch `LC_CTYPE`, so unrelated threads can observe transient or permanent locale changes."
url = "https://docs.python.org/3.14/library/locale.html#background-details-hints-tips-and-caveats"
obscurity_bucket = 8
obscurity_rating = 6.70
cursedness_rating = 7.47

[[items]]
slug = "sqlite-s-default-timestamp-converter-discards-utc-offsets"
name = "SQLite timestamp offset loss"
short_description = "The built-in SQLite timestamp converter ignores UTC offsets stored in values and returns a naive `datetime`. The default adapters and converters are deprecated, but legacy code can silently turn offset-aware database data into ambiguous local-looking objects."
url = "https://docs.python.org/3.14/library/sqlite3.html#default-adapters-and-converters-deprecated"
obscurity_bucket = 8
obscurity_rating = 7.00
cursedness_rating = 7.43

[[items]]
slug = "ast-literal-eval-can-cause-denial-of-service"
name = "The `ast.literal_eval()` denial of service"
short_description = "`literal_eval` avoids general code execution, but it is not safe for arbitrary hostile input. Small crafted expressions can exhaust memory or the C stack, consume excessive CPU, or crash the process."
url = "https://docs.python.org/3.14/library/ast.html#ast.literal_eval"
obscurity_bucket = 8
obscurity_rating = 6.77
cursedness_rating = 7.43

[[items]]
slug = "argparse-filetype-can-truncate-output-before-parsing-later-arguments"
name = "The `argparse.FileType` truncation trap"
short_description = "`FileType` opens files as soon as its argument is converted. An output file may therefore be created or truncated before a later command-line argument fails validation, and the parser does not automatically close that file on the subsequent error."
url = "https://docs.python.org/3.14/library/argparse.html#filetype-objects"
obscurity_bucket = 8
obscurity_rating = 7.00
cursedness_rating = 7.27

[[items]]
slug = "letting-a-multiprocessing-pool-die-by-garbage-collection-can-hang-shutdown"
name = "The multiprocessing Pool finalizer hang"
short_description = "`Pool` resources must be closed or terminated explicitly, preferably with a context manager. CPython does not guarantee that the pool finalizer will run, and the documentation warns that relying on collection can leave the process hanging during finalization."
url = "https://docs.python.org/3.14/library/multiprocessing.html#module-multiprocessing.pool"
obscurity_bucket = 8
obscurity_rating = 6.97
cursedness_rating = 7.20

[[items]]
slug = "itertools-tee-can-buffer-enormous-amounts-and-is-not-threadsafe"
name = "The `itertools.tee()` buffer trap"
short_description = "Independent-looking tee iterators share a hidden data chain. If one consumer races far ahead, memory grows with the gap; using sibling iterators concurrently can also raise `RuntimeError`."
url = "https://docs.python.org/3.14/library/itertools.html#itertools.tee"
obscurity_bucket = 8
obscurity_rating = 6.47
cursedness_rating = 7.17

[[items]]
slug = "bytecode-caching-trusts-a-one-second-timestamp"
name = "The stale timestamp-based `.pyc` trap"
short_description = "The default `.pyc` invalidation compares source mtime and size. Edit a file twice within the same second, or check it out with a rewritten timestamp, and Python happily runs the stale bytecode."
url = "https://peps.python.org/pep-0552/"
obscurity_bucket = 8
obscurity_rating = 7.00
cursedness_rating = 7.00

[[items]]
slug = "logging-config-listen-can-execute-attacker-controlled-code"
name = "The logging configuration server RCE"
short_description = "The socket listener accepts logging configurations whose processing may use `eval`, import objects, and call constructors. On a multi-user machine, an untrusted local user able to connect can potentially execute code in the listening process."
url = "https://docs.python.org/3.14/library/logging.config.html#logging.config.listen"
obscurity_bucket = 9
obscurity_rating = 7.80
cursedness_rating = 9.40

[[items]]
slug = "logging-from-handler-emit-can-deadlock-the-logging-system"
name = "The logging lock-inversion deadlock"
short_description = "`Handler.handle()` calls `emit()` while holding a handler-level lock. If custom `emit()` code invokes an API that takes the module-level logging lock while another thread configures logging in the opposite lock order, both threads can deadlock."
url = "https://docs.python.org/3.14/library/logging.html#logging.Handler.emit"
obscurity_bucket = 9
obscurity_rating = 7.63
cursedness_rating = 8.57

[[items]]
slug = "zipfile-path-does-not-sanitize-archive-filenames"
name = "`zipfile.Path` traversal"
short_description = "Unlike safer high-level extraction helpers, `zipfile.Path` leaves filename validation to the caller. Code must prevent absolute paths and `..` traversal or archive entries can escape the intended directory."
url = "https://docs.python.org/3.14/library/zipfile.html#zipfile.Path"
obscurity_bucket = 9
obscurity_rating = 7.53
cursedness_rating = 8.53

[[items]]
slug = "signal-handler-exceptions-materialize-out-of-thin-air-in-unrelated-code"
name = "Out-of-thin-air signal exceptions"
short_description = "A low-level signal handler only sets a flag; the Python handler runs later at the next bytecode opportunity. If it raises, the exception can appear after an arbitrary operation in the main thread rather than at the point where the signal arrived."
url = "https://docs.python.org/3.14/library/signal.html#execution-of-python-signal-handlers"
obscurity_bucket = 9
obscurity_rating = 7.27
cursedness_rating = 7.77

[[items]]
slug = "exec-with-separate-globals-and-locals-has-class-body-scoping"
name = "The dual-namespace `exec()` trap"
short_description = "When different mappings are supplied for globals and locals, the executed code behaves as though it were a class body. Top-level assignments go into the locals mapping, but nested functions do not close over those names and will search the globals mapping instead."
url = "https://docs.python.org/3.14/library/functions.html#exec"
obscurity_bucket = 9
obscurity_rating = 7.70
cursedness_rating = 7.33

[[items]]
slug = "asyncio-wait-for-can-outlive-its-timeout"
name = "The soft `asyncio.wait_for()` timeout"
short_description = "On timeout, `wait_for()` cancels the awaitable and then waits for cancellation to complete. Cleanup that is slow, blocked, or cancellation-resistant can make total elapsed time exceed the advertised timeout."
url = "https://docs.python.org/3.14/library/asyncio-task.html#asyncio.wait_for"
obscurity_bucket = 9
obscurity_rating = 7.03
cursedness_rating = 7.30

[[items]]
slug = "a-lock-from-one-multiprocessing-context-may-break-in-another"
name = "Mixed multiprocessing contexts"
short_description = "Synchronization primitives belong to the context that created them. The documentation warns that locks produced under the `fork` context cannot generally be passed to processes created with `spawn` or `forkserver`, turning a configuration choice into an object-compatibility constraint."
url = "https://docs.python.org/3.14/library/multiprocessing.html#contexts-and-start-methods"
obscurity_bucket = 9
obscurity_rating = 7.60
cursedness_rating = 7.23

[[items]]
slug = "python-audit-hooks-are-explicitly-not-a-sandbox"
name = "Audit hooks are not a sandbox"
short_description = "The documentation warns that audit hooks are not suitable for implementing a sandbox. Malicious code can bypass or disable hooks added from Python; security-sensitive native hooks must be installed before runtime initialization and still do not turn Python into a safe evaluator."
url = "https://docs.python.org/3.14/library/sys.html#sys.addaudithook"
obscurity_bucket = 9
obscurity_rating = 7.27
cursedness_rating = 7.23

[[items]]
slug = "list-sort-can-make-the-list-look-empty-while-sorting"
name = "The empty-list sort sentinel"
short_description = "CPython temporarily makes a list appear empty while its in-place sort runs. Inspecting or mutating it from comparison code is undefined, and detected mutation may cause `ValueError`."
url = "https://docs.python.org/3.14/library/stdtypes.html#list.sort"
obscurity_bucket = 9
obscurity_rating = 7.60
cursedness_rating = 7.20

[[items]]
slug = "a-subclass-can-hide-a-base-slot-with-another-slot-of-the-same-name"
name = "Duplicate `__slots__` shadowing"
short_description = "If a subclass repeats a slot name used by a base class, the new descriptor hides the old one. The base slot remains reachable only through its original descriptor, and the documentation says the program's meaning is undefined and may be rejected in a future version."
url = "https://docs.python.org/3.14/reference/datamodel.html#object.__slots__"
obscurity_bucket = 9
obscurity_rating = 7.73
cursedness_rating = 6.97

[[items]]
slug = "popen-user-can-retain-supplementary-group-privileges"
name = "Incomplete subprocess privilege dropping"
short_description = "On POSIX, `Popen(user=...)` changes the child UID but does not automatically clear the inherited supplementary-group list. Unless `extra_groups` is set appropriately, an apparent privilege drop can retain filesystem or device access granted through the parent's groups."
url = "https://docs.python.org/3.14/library/subprocess.html#popen-constructor"
obscurity_bucket = 10
obscurity_rating = 8.43
cursedness_rating = 9.67

[[items]]
slug = "py-decref-can-execute-arbitrary-python-code"
name = "`Py_DECREF` re-entrancy"
short_description = "Dropping the last reference invokes deallocation, which can run `__del__` and therefore arbitrary Python with access to globals. C extensions must restore every externally visible invariant before decrementing a reference, because cleanup itself can re-enter the program."
url = "https://docs.python.org/3.14/c-api/refcounting.html#c.Py_DECREF"
obscurity_bucket = 10
obscurity_rating = 8.70
cursedness_rating = 9.23

[[items]]
slug = "macos-dbm-ndbm-can-corrupt-databases-and-segfault-python"
name = "The macOS `dbm.ndbm` corruption bug"
short_description = "Apple's bundled NDBM has an undocumented value-size limit. Writing a larger value can corrupt the database file, and merely reading the corrupted file can cause a hard interpreter crash by segmentation fault."
url = "https://docs.python.org/3.14/library/dbm.html#module-dbm.ndbm"
obscurity_bucket = 10
obscurity_rating = 9.00
cursedness_rating = 9.10

[[items]]
slug = "pylist-setitem-steals-your-reference-even-when-it-fails"
name = "The `PyList_SetItem` stolen-reference trap"
short_description = "Most C APIs leave ownership with the caller when an operation fails. `PyList_SetItem` is different: it steals the incoming reference even on an out-of-range error, so ordinary error-cleanup code can double-decrement an object and corrupt memory."
url = "https://docs.python.org/3.14/c-api/list.html#c.PyList_SetItem"
obscurity_bucket = 10
obscurity_rating = 8.83
cursedness_rating = 8.97

[[items]]
slug = "an-encoding-comment-can-hide-code-from-your-editor"
name = "Source-encoding deception"
short_description = "PEP 263 lets the first two lines declare any codec, including `unicode_escape`. Under it, escape sequences in the file become real quotes and braces, so linters and syntax highlighters see a string where the compiler sees executable code."
url = "https://peps.python.org/pep-0672/"
obscurity_bucket = 10
obscurity_rating = 7.87
cursedness_rating = 8.97

[[items]]
slug = "length-hint-can-cause-buffer-overflows"
name = "Malicious `__length_hint__` overflow"
short_description = "A custom `__length_hint__` returning zero can trick `bytearray_extend` into reusing a shared empty string, causing a global buffer overflow (CVE-2025)."
url = "https://github.com/python/cpython/issues/143003"
obscurity_bucket = 10
obscurity_rating = 8.60
cursedness_rating = 8.90

[[items]]
slug = "independent-shared-memory-trackers-can-delete-memory-still-in-use"
name = "The shared-memory tracker race"
short_description = "On POSIX, independently launched Python processes can have separate resource trackers. The first tracker to exit may unlink a shared-memory block while unrelated processes still use it; Python 3.13 added `track=False` for externally managed lifetimes."
url = "https://docs.python.org/3.14/library/multiprocessing.shared_memory.html#multiprocessing.shared_memory.SharedMemory"
obscurity_bucket = 10
obscurity_rating = 8.57
cursedness_rating = 8.73

[[items]]
slug = "threading-local-slots-are-secretly-shared-across-threads"
name = "Shared `threading.local` slots"
short_description = "Subclassing `threading.local` and declaring `__slots__` does not create thread-local slots. Those slot attributes are shared across all threads, even while ordinary attributes in the same object remain thread-specific."
url = "https://docs.python.org/3.14/library/threading.html#thread-local-data"
obscurity_bucket = 10
obscurity_rating = 8.30
cursedness_rating = 8.07

[[items]]
slug = "an-existing-audit-hook-can-silently-veto-installation-of-a-new-one"
name = "Silent audit-hook installation veto"
short_description = "Adding an audit hook first emits a `sys.addaudithook` audit event to existing hooks. If one raises `RuntimeError` or a subclass, the new hook is not added and that exception is suppressed, so code may believe monitoring was installed when it was not."
url = "https://docs.python.org/3.14/library/sys.html#sys.addaudithook"
obscurity_bucket = 10
obscurity_rating = 8.60
cursedness_rating = 8.00

[[items]]
slug = "socket-makefile-timeouts-can-leave-its-buffer-inconsistent"
name = "The poisoned `socket.makefile()` buffer"
short_description = "A file object returned by `socket.makefile()` has internal buffering. If the socket times out, that buffer may be left inconsistent and later reads can fail or lose synchronization, even though the underlying socket still exists."
url = "https://docs.python.org/3.14/library/socket.html#socket.socket.makefile"
obscurity_bucket = 10
obscurity_rating = 7.93
cursedness_rating = 7.97

[[items]]
slug = "foreign-thread-ctypes-callbacks-lose-threading-local-state-every-call"
name = "Foreign-thread `ctypes` callback state loss"
short_description = "When C invokes a `CFUNCTYPE` callback from a thread created outside Python, `ctypes` creates a fresh dummy Python thread state on every callback. Values stored in `threading.local` do not survive between calls—even when the same native thread invokes them repeatedly."
url = "https://docs.python.org/3.14/library/ctypes.html#callback-functions"
obscurity_bucket = 10
obscurity_rating = 8.67
cursedness_rating = 7.87

[[items]]
slug = "a-custom-shutil-archiver-can-chdir-the-entire-process"
name = "The `shutil.make_archive()` chdir race"
short_description = "If a registered archive function does not declare support for `root_dir`, `make_archive()` temporarily changes the process working directory before calling it. That compatibility path is not thread-safe and can redirect unrelated relative-path operations in other threads."
url = "https://docs.python.org/3.14/library/shutil.html#shutil.make_archive"
obscurity_bucket = 10
obscurity_rating = 8.20
cursedness_rating = 7.77

[[items]]
slug = "weakkeydictionary-can-delete-a-replacement-value-when-the-old-key-dies"
name = "The `WeakKeyDictionary` replacement-key trap"
short_description = "Assigning with an equal but non-identical key replaces the value while retaining the original key object. When that original key is collected, the entry disappears—even if the newer equal key is still alive."
url = "https://docs.python.org/3.14/library/weakref.html#weakref.WeakKeyDictionary"
obscurity_bucket = 10
obscurity_rating = 8.10
cursedness_rating = 7.73

[[items]]
slug = "zoneinfo-cache-identity-can-change-datetime-semantics"
name = "The `ZoneInfo` identity trap"
short_description = "`ZoneInfo(key)` normally returns a cached singleton. Datetime comparison and subtraction use different rules when two `tzinfo` attributes are the same object versus merely equivalent objects, so `ZoneInfo.no_cache()` or `clear_cache()` can change results without changing either zone key."
url = "https://docs.python.org/3.14/library/zoneinfo.html#zoneinfo.ZoneInfo.clear_cache"
obscurity_bucket = 10
obscurity_rating = 8.10
cursedness_rating = 7.60

[[items]]
slug = "swapping-ctypes-structure-fields-can-duplicate-one-value"
name = "The `ctypes` structure-swap trap"
short_description = "Accessing a nested `ctypes` field returns a wrapper onto the parent's buffer, not an independent copy. In `s.a, s.b = s.b, s.a`, the first assignment mutates the storage observed by the second temporary, so both fields can end up with the same value."
url = "https://docs.python.org/3.14/library/ctypes.html#surprises"
obscurity_bucket = 10
obscurity_rating = 8.57
cursedness_rating = 7.57

[[items]]
slug = "os-reload-environ-can-make-the-environment-appear-empty-mid-read"
name = "The `os.reload_environ()` empty-read race"
short_description = "The new reload function refreshes `os.environ` and `os.environb`, but it is explicitly not thread-safe. Reading either mapping while another thread reloads can return an empty result rather than the real environment."
url = "https://docs.python.org/3.14/library/os.html#os.reload_environ"
obscurity_bucket = 10
obscurity_rating = 8.30
cursedness_rating = 7.57
